"use client";

import { findProduct, ProductImage, products, rupiah } from "./catalog";
import { useState } from "react";
import { ArrowLeft, ArrowUpRight, Check, LockKeyhole, Plus, ShoppingBag, UserRound, X } from "lucide-react";
import Admin from "./admin";

type View = "shop" | "product" | "checkout" | "success" | "track" | "account";
type CartItem = { id: number; qty: number; variant: string };

export default function Home() {
  const [view, setView] = useState<View>("shop");
  const [admin, setAdmin] = useState(false);
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(products[0]);
  const [variant, setVariant] = useState("All Size");
  const [qty, setQty] = useState(1);
  const [cart, setCart] = useState<CartItem[]>([{ id: 1, qty: 1, variant: "All Size" }, { id: 2, qty: 1, variant: "One Size" }]);
  const [cartOpen, setCartOpen] = useState(false);
  const [promoInput, setPromoInput] = useState("");
  const [promo, setPromo] = useState("");
  const [promoMessage, setPromoMessage] = useState("");
  const [shipping, setShipping] = useState(18000);
  const [payment, setPayment] = useState("QRIS");
  const [toast, setToast] = useState("");
  const [trackingNumber, setTrackingNumber] = useState("LS-260917-084");
  const [trackingFound, setTrackingFound] = useState(true);
  const subtotal = cart.reduce((sum, item) => sum + findProduct(item.id).price * item.qty, 0);
  const count = cart.reduce((sum, item) => sum + item.qty, 0);
  const discount = promo === "WELCOME10" ? Math.min(Math.round(subtotal * .1), 40000) : promo === "LUMIERE15" ? Math.round(cart.filter((item) => item.id === 1).reduce((sum, item) => sum + findProduct(item.id).price * item.qty, 0) * .15) : 0;
  const total = subtotal + shipping - discount;
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(""), 2300); };
  const showView = (next: View) => { setView(next); setCartOpen(false); window.scrollTo(0, 0); };
  const chooseCategory = (next: string) => { setCategory(next); showView("shop"); };
  const openProduct = (product: Product) => { setSelected(product); setVariant(product.id === 2 ? "One Size" : "All Size"); setQty(1); showView("product"); };
  const addToCart = (product: Product, amount = 1, chosenVariant = "All Size") => {
    setCart((items) => { const match = items.find((item) => item.id === product.id && item.variant === chosenVariant); return match ? items.map((item) => item === match ? { ...item, qty: Math.min(product.stock, item.qty + amount) } : item) : [...items, { id: product.id, qty: Math.min(product.stock, amount), variant: chosenVariant }]; });
    notify(`${product.name} added to bag`);
  };
  const changeQty = (item: CartItem, amount: number) => setCart((items) => items.map((entry) => entry === item ? { ...entry, qty: Math.min(findProduct(entry.id).stock, entry.qty + amount) } : entry).filter((entry) => entry.qty > 0));
  const applyPromo = () => { const code = promoInput.trim().toUpperCase(); if (code === "WELCOME10" || (code === "LUMIERE15" && cart.some((item) => item.id === 1))) { setPromo(code); setPromoMessage(code === "WELCOME10" ? "WELCOME10 applied — 10% off, capped at Rp 40.000." : "LUMIERE15 applied to eligible Lumière item."); notify("Promo code applied"); } else { setPromo(""); setPromoMessage("Promo code is invalid, expired, or does not meet the requirements."); } };
  const field = "w-full bg-transparent border border-black/20 p-4 outline-none focus:border-ink";

  return <>
    {toast && <div role="status" className="fixed top-5 left-1/2 -translate-x-1/2 z-[100] rounded-full bg-ink px-5 py-3 text-sm text-white shadow-xl">{toast}</div>}
    {admin ? <Admin onStore={() => { setAdmin(false); showView("shop"); }} notify={notify} /> : <div>
      <div className="bg-ink text-white text-center text-[11px] tracking-[.2em] uppercase py-2">Free shipping Jabodetabek for orders over Rp 300.000</div>
      <header className="sticky top-0 z-40 glass border-b border-black/10"><div className="max-w-[1500px] mx-auto px-5 md:px-9 h-20 flex items-center justify-between gap-6"><button onClick={() => showView("shop")} className="font-serif text-2xl md:text-3xl tracking-tight whitespace-nowrap">Lady Shopia <span className="text-black/35 font-sans text-sm align-middle ml-1">Shop</span></button><nav className="hidden lg:flex items-center gap-7 text-sm"><button onClick={() => chooseCategory("All")}>All Products</button><button onClick={() => chooseCategory("Dress")}>Dress</button><button onClick={() => chooseCategory("One Set")}>One Set</button><button onClick={() => chooseCategory("Premium")}>Premium Series</button><button onClick={() => showView("track")}>Track Order</button></nav><div className="flex items-center gap-4"><button onClick={() => showView("account")} aria-label="Account"><UserRound size={20} /></button><button onClick={() => setCartOpen(true)} className="relative" aria-label="Open bag"><ShoppingBag size={20} /><span className="absolute -right-2 -top-2 w-5 h-5 rounded-full bg-wine text-white text-[10px] grid place-items-center">{count}</span></button><button onClick={() => setAdmin(true)} className="hidden md:inline-flex border border-black/20 rounded-full px-3 py-1.5 text-[11px] uppercase tracking-widest">Admin demo</button></div></div></header>
      <div className="border-b border-black/10 bg-white/35"><div className="max-w-[1500px] mx-auto px-5 md:px-9 py-3 flex flex-col sm:flex-row sm:items-center justify-between gap-2 text-xs"><span className="text-black/50">You are now in the Lady Shopia online shop.</span><a href="https://lady-shopia-app.vercel.app/" className="inline-flex items-center gap-2 font-medium hover:opacity-60">Back to Lady Shopia storefront <ArrowUpRight size={14} /></a></div></div>
      {view === "shop" && <main><section className="max-w-[1500px] mx-auto px-5 md:px-9 py-12 md:py-16"><div className="flex flex-col md:flex-row md:items-end justify-between gap-6 border-b border-black/15 pb-8"><div><p className="text-[11px] uppercase tracking-[.22em] text-black/45">Official Online Shop</p><h1 className="font-serif text-5xl md:text-7xl mt-2">Shop Lady Shopia.</h1><p className="text-black/50 mt-4 max-w-2xl">Choose your item, check live stock, add it to your bag, and complete the order directly online.</p></div><div className="flex gap-2 overflow-auto hide-scrollbar">{["All", "Dress", "One Set", "Premium"].map((item) => <button key={item} onClick={() => setCategory(item)} className={`${category === item ? "bg-ink text-white" : "border border-black/20"} rounded-full px-5 py-2.5 text-sm whitespace-nowrap`}>{item}</button>)}</div></div><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-x-4 gap-y-12 mt-10">{products.filter((product) => category === "All" || product.category === category).map((product) => <article key={product.id} className="group"><div className="relative aspect-[4/5] overflow-hidden bg-sand"><button onClick={() => openProduct(product)} className="absolute inset-0 w-full" aria-label={`View ${product.name}`}><ProductImage product={product} className="group-hover:scale-[1.025] transition duration-500" /></button>{product.stock <= 3 && <span className="absolute top-3 left-3 bg-wine text-white px-3 py-1.5 text-[10px] uppercase tracking-widest">Low stock</span>}<button onClick={() => addToCart(product)} aria-label={`Add ${product.name} to bag`} className="absolute right-3 bottom-3 w-11 h-11 rounded-full bg-cream grid place-items-center shadow-sm"><Plus size={16} /></button></div><div className="mt-4 flex justify-between gap-4"><div><p className="text-xs uppercase tracking-widest text-black/40">{product.series}</p><button onClick={() => openProduct(product)} className="font-serif text-2xl mt-1 text-left">{product.name}</button></div><strong className="font-medium whitespace-nowrap">{rupiah(product.price)}</strong></div></article>)}</div></section></main>}
      {view === "product" && <main><section className="max-w-[1500px] mx-auto px-5 md:px-9 py-10"><button onClick={() => showView("shop")} className="text-sm flex items-center gap-2 mb-8"><ArrowLeft size={16} /> Back to catalog</button><div className="grid lg:grid-cols-[1.08fr_.92fr] gap-10 lg:gap-16"><div className="grid grid-cols-[65px_1fr] sm:grid-cols-[78px_1fr] gap-4"><div className="space-y-3">{[0, 1, 2].map((index) => <div key={index} className={`relative w-full aspect-[3/4] border ${index === 0 ? "border-black" : "border-transparent"} overflow-hidden bg-sand`}><ProductImage product={selected} className={index === 1 ? "scale-110" : ""} sizes="80px" /></div>)}</div><div className="relative aspect-[4/5] bg-sand overflow-hidden"><ProductImage product={selected} sizes="(max-width: 1024px) 80vw, 45vw" /></div></div><div className="lg:pt-8"><div className="flex justify-between text-xs uppercase tracking-widest text-black/45"><span>{selected.series}</span><span>{selected.sku}</span></div><h1 className="font-serif text-5xl md:text-6xl mt-5">{selected.name}</h1><div className="text-xl mt-5">{rupiah(selected.price)}</div><p className="text-black/60 leading-7 mt-7 max-w-xl">{selected.desc}</p><div className="mt-10"><div className="flex justify-between mb-3"><span className="text-sm">Variant</span><span className={`text-xs ${selected.stock <= 3 ? "text-wine" : "text-green-700"}`}>{selected.stock <= 3 ? `Only ${selected.stock} left` : `In stock · ${selected.stock} pcs`}</span></div><div className="grid grid-cols-3 gap-2">{["All Size", "M / L", "XL"].map((choice) => <button key={choice} onClick={() => setVariant(choice)} className={`border ${variant === choice ? "border-black" : "border-black/15"} p-4 text-sm`}>{choice}</button>)}</div></div><div className="mt-8 flex gap-3"><div className="border border-black flex items-center"><button onClick={() => setQty(Math.max(1, qty - 1))} className="px-4 py-4" aria-label="Decrease quantity">−</button><span className="min-w-8 text-center">{qty}</span><button onClick={() => setQty(Math.min(selected.stock, qty + 1))} className="px-4 py-4" aria-label="Increase quantity">+</button></div><button onClick={() => addToCart(selected, qty, variant)} className="flex-1 bg-ink text-white px-5 py-4">Add to bag</button></div><div className="mt-10 divide-y divide-black/10 border-y border-black/10"><details className="py-5" open><summary className="cursor-pointer font-medium">Product details</summary><p className="text-sm text-black/55 leading-6 mt-3">Soft-touch fabric, relaxed silhouette and lightweight finish designed for daily wear.</p></details><details className="py-5"><summary className="cursor-pointer font-medium">Shipping & returns</summary><p className="text-sm text-black/55 leading-6 mt-3">Orders are processed in 1–2 working days. Shipping cost is calculated at checkout.</p></details></div></div></div></section></main>}
      {view === "checkout" && <main><section className="max-w-6xl mx-auto px-5 md:px-9 py-12"><div className="flex items-center justify-between border-b border-black/10 pb-7"><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Secure checkout</p><h1 className="font-serif text-5xl mt-2">Almost yours.</h1></div><LockKeyhole size={24} /></div><form onSubmit={(event) => { event.preventDefault(); showView("success"); notify("Order LS-260917-084 created"); }} className="grid lg:grid-cols-[1.15fr_.85fr] gap-12 mt-10"><div className="space-y-9"><section><h2 className="font-serif text-2xl">1. Contact</h2><div className="grid md:grid-cols-2 gap-3 mt-4"><input required className={field} placeholder="Full name" aria-label="Full name" /><input required type="tel" className={field} placeholder="Phone / WhatsApp" aria-label="Phone or WhatsApp" /></div></section><section><h2 className="font-serif text-2xl">2. Delivery address</h2><div className="grid gap-3 mt-4"><textarea required className={`${field} h-28`} placeholder="Street, building, district, city" aria-label="Street address" /><div className="grid md:grid-cols-2 gap-3"><input required className={field} placeholder="Postal code" aria-label="Postal code" /><select className={field} aria-label="Province"><option>DKI Jakarta</option><option>Jawa Barat</option><option>Jawa Tengah</option></select></div></div></section><section><h2 className="font-serif text-2xl">3. Shipping</h2><div className="space-y-2 mt-4">{[[18000, "J&T Regular · 2–3 days"], [25000, "SiCepat BEST · 1–2 days"]].map(([cost, label]) => <label key={cost} className={`flex justify-between border ${shipping === cost ? "border-black" : "border-black/15"} p-4 gap-3`}><span><input type="radio" name="ship" checked={shipping === cost} onChange={() => setShipping(Number(cost))} className="mr-3" />{label}</span><strong>{rupiah(Number(cost))}</strong></label>)}</div></section><section><h2 className="font-serif text-2xl">4. Payment</h2><div className="grid sm:grid-cols-3 gap-2 mt-4">{[["QRIS", "Instant"], ["Bank Transfer", "BCA / Mandiri"], ["E-Wallet", "GoPay / OVO"]].map(([name, detail]) => <button type="button" key={name} onClick={() => setPayment(name)} className={`border ${payment === name ? "border-2 border-black" : "border-black/15"} p-5 text-left`}><strong>{name}</strong><span className="block text-xs text-black/45 mt-1">{detail}</span></button>)}</div></section></div><aside className="lg:sticky lg:top-28 h-fit bg-white/45 p-6 md:p-8"><h2 className="font-serif text-3xl">Order summary</h2><div className="mt-6 space-y-5">{cart.map((item) => { const product = findProduct(item.id); return <div key={`${item.id}-${item.variant}`} className="flex gap-4"><div className="relative w-20 h-24 shrink-0"><ProductImage product={product} sizes="80px" /></div><div className="flex-1"><strong className="font-medium">{product.name}</strong><p className="text-xs text-black/50 mt-1">{item.variant} · Qty {item.qty}</p><p className="mt-4">{rupiah(product.price * item.qty)}</p></div></div>; })}</div><div className="border-t border-black/10 mt-7 pt-5"><label htmlFor="promo" className="text-xs uppercase tracking-widest text-black/45">Promo code</label><div className="flex mt-2"><input id="promo" value={promoInput} onChange={(event) => setPromoInput(event.target.value)} placeholder="e.g. WELCOME10" className="min-w-0 flex-1 bg-transparent border border-black/20 px-4 py-3 text-sm uppercase" /><button type="button" onClick={applyPromo} className="border-y border-r border-black/20 px-4 text-sm">Apply</button></div>{promoMessage && <p className={`text-xs mt-2 ${promo ? "text-green-700" : "text-wine"}`}>{promoMessage}</p>}</div><div className="border-t border-black/10 mt-7 pt-5 text-sm space-y-3"><div className="flex justify-between"><span>Subtotal</span><span>{rupiah(subtotal)}</span></div>{discount > 0 && <div className="flex justify-between text-green-700"><span>Discount · {promo}</span><span>− {rupiah(discount)}</span></div>}<div className="flex justify-between"><span>Shipping</span><span>{rupiah(shipping)}</span></div><div className="flex justify-between text-lg font-medium pt-3"><span>Total</span><span>{rupiah(total)}</span></div></div><button type="submit" disabled={!cart.length} className="w-full bg-ink text-white py-5 mt-7 disabled:opacity-40">Place order — {rupiah(total)}</button></aside></form></section></main>}
      {view === "success" && <main><section className="max-w-3xl mx-auto px-5 md:px-9 py-24 text-center"><div className="w-20 h-20 rounded-full bg-sage mx-auto grid place-items-center"><Check size={32} /></div><p className="text-xs uppercase tracking-[.25em] text-black/45 mt-8">Order LS-260917-084</p><h1 className="font-serif text-6xl md:text-7xl mt-3">Thank you.</h1><p className="text-black/55 leading-7 mt-6">Your order has been received. We’ll send a WhatsApp update when your package is ready to ship.</p><div className="grid sm:grid-cols-3 border border-black/15 mt-10 text-left"><div className="p-5 border-b sm:border-b-0 sm:border-r border-black/15"><span className="text-xs text-black/45">Payment</span><strong className="block mt-1">Paid · {payment}</strong></div><div className="p-5 border-b sm:border-b-0 sm:border-r border-black/15"><span className="text-xs text-black/45">Shipping</span><strong className="block mt-1">{shipping === 18000 ? "J&T Regular" : "SiCepat BEST"}</strong></div><div className="p-5"><span className="text-xs text-black/45">ETA</span><strong className="block mt-1">20 Sep 2026</strong></div></div><div className="flex flex-wrap justify-center gap-3 mt-8"><button onClick={() => showView("track")} className="bg-ink text-white px-7 py-4">Track this order</button><button onClick={() => showView("shop")} className="border border-black/20 px-7 py-4">Continue shopping</button></div></section></main>}
      {view === "track" && <main><section className="max-w-5xl mx-auto px-5 md:px-9 py-14"><p className="text-xs uppercase tracking-[.25em] text-black/45">Order tracking</p><h1 className="font-serif text-6xl mt-2">Where is my order?</h1><div className="grid lg:grid-cols-[.8fr_1.2fr] gap-9 mt-12"><div className="bg-white/45 p-7 h-fit"><label htmlFor="order-number" className="text-xs uppercase tracking-widest">Order number</label><form onSubmit={(event) => { event.preventDefault(); setTrackingFound(trackingNumber.trim().toUpperCase() === "LS-260917-084"); }} className="flex mt-3"><input id="order-number" value={trackingNumber} onChange={(event) => setTrackingNumber(event.target.value)} className="min-w-0 flex-1 bg-transparent border border-black/20 p-4" /><button className="bg-ink text-white px-5">Track</button></form>{trackingFound && <div className="border-t border-black/10 mt-7 pt-6 text-sm space-y-3"><div className="flex justify-between"><span className="text-black/45">Courier</span><strong>J&T Express</strong></div><div className="flex justify-between"><span className="text-black/45">Tracking no.</span><strong>JP8392041571</strong></div><div className="flex justify-between"><span className="text-black/45">ETA</span><strong>20 Sep 2026</strong></div></div>}</div><div>{trackingFound ? <><div className="flex items-center justify-between"><div><p className="text-sm text-black/45">Current status</p><h2 className="font-serif text-4xl mt-1">On the way.</h2></div><span className="rounded-full bg-sage px-4 py-2 text-sm">In transit</span></div><div className="relative mt-10 pl-8 before:absolute before:left-[9px] before:top-2 before:bottom-5 before:w-px before:bg-black/20 space-y-9">{[["Package departed sorting center", "Jakarta Selatan · 18 Sep, 20:42"], ["Picked up by J&T Express", "Lady Shopia warehouse · 18 Sep, 14:15"], ["Order packed", "17 Sep, 17:04"], ["Delivered", "Expected 20 Sep"]].map(([title, detail], index) => <div key={title} className={`relative ${index === 3 ? "opacity-40" : ""}`}><span className={`absolute -left-8 top-1 w-[19px] h-[19px] rounded-full ${index === 3 ? "bg-cream border border-black/40" : "bg-ink border-4 border-cream"}`} /><strong>{title}</strong><p className="text-sm text-black/45 mt-1">{detail}</p></div>)}</div></> : <p className="text-black/55">No demo order found for that number.</p>}</div></div></section></main>}
      {view === "account" && <main><section className="max-w-6xl mx-auto px-5 md:px-9 py-14"><div className="flex flex-col md:flex-row md:items-end justify-between gap-5"><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Customer account</p><h1 className="font-serif text-6xl mt-2">Hello, Ayu.</h1></div><button onClick={() => notify("Profile editing is not part of this demo")} className="border border-black/20 px-5 py-3 text-sm">Edit profile</button></div><div className="grid lg:grid-cols-[.7fr_1.3fr] gap-8 mt-10"><aside className="border border-black/15 p-6 h-fit"><p className="text-xs text-black/45">MEMBER SINCE</p><p className="mt-1">August 2026</p><div className="border-t border-black/10 my-5" /><p className="text-xs text-black/45">DEFAULT ADDRESS</p><p className="text-sm leading-6 mt-1">Jl. Melati 18, Tebet<br />Jakarta Selatan 12810</p></aside><div><h2 className="font-serif text-3xl">Recent orders</h2><div className="mt-5 border-t border-black/15"><div className="py-6 border-b border-black/15 grid md:grid-cols-[1fr_auto] gap-5"><div className="flex gap-4"><div className="relative w-20 h-24 shrink-0"><ProductImage product={products[0]} sizes="80px" /></div><div><strong>LS-260917-084</strong><p className="text-sm text-black/45 mt-1">2 items · Rp 437.000</p><span className="inline-block bg-sage rounded-full px-3 py-1 text-xs mt-4">In transit</span></div></div><button onClick={() => showView("track")} className="self-center text-sm border-b border-black pb-1">Track order →</button></div><div className="py-6 border-b border-black/15 grid md:grid-cols-[1fr_auto] gap-5"><div><strong>LS-260824-032</strong><p className="text-sm text-black/45 mt-1">1 item · Rp 95.000</p><span className="inline-block bg-black/5 rounded-full px-3 py-1 text-xs mt-4">Delivered</span></div><button onClick={() => addToCart(products[3])} className="self-center text-sm border-b border-black pb-1">Buy again →</button></div></div></div></div></section></main>}
      <footer className="border-t border-black/10 mt-16"><div className="max-w-[1500px] mx-auto px-5 md:px-9 py-10 flex flex-col md:flex-row gap-6 md:items-center justify-between"><div><div className="font-serif text-2xl">Lady Shopia Shop</div><p className="text-sm text-black/45 mt-1">Official transactional shop for Lady Shopia.</p></div><div className="flex flex-wrap gap-5 text-sm text-black/55"><button onClick={() => showView("track")}>Track Order</button><button onClick={() => showView("account")}>My Orders</button><a href="https://lady-shopia-app.vercel.app/" className="hover:text-black">Storefront</a><span>WhatsApp Support</span></div></div></footer>
      {cartOpen && <><button aria-label="Close bag" onClick={() => setCartOpen(false)} className="fixed inset-0 bg-black/25 z-50 w-full" /><aside role="dialog" aria-modal="true" aria-label="Your bag" className="fixed right-0 top-0 bottom-0 z-[60] w-full sm:w-[470px] bg-cream shadow-2xl p-6 md:p-8 flex flex-col"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Your bag</p><h2 className="font-serif text-4xl mt-1">{count} items</h2></div><button onClick={() => setCartOpen(false)} aria-label="Close bag" className="w-10 h-10 grid place-items-center border border-black/15 rounded-full"><X /></button></div><div className="flex-1 overflow-auto mt-8 space-y-6">{cart.length ? cart.map((item) => { const product = findProduct(item.id); return <div key={`${item.id}-${item.variant}`} className="flex gap-4"><div className="relative w-24 h-32 shrink-0"><ProductImage product={product} sizes="96px" /></div><div className="flex-1"><div className="flex justify-between gap-4"><strong className="font-medium">{product.name}</strong><strong className="whitespace-nowrap">{rupiah(product.price * item.qty)}</strong></div><p className="text-xs text-black/45 mt-1">{item.variant}</p><div className="flex justify-between items-end mt-6"><div className="border border-black/20 inline-flex"><button onClick={() => changeQty(item, -1)} aria-label={`Decrease ${product.name}`} className="px-3 py-2">−</button><span className="px-2 py-2">{item.qty}</span><button onClick={() => changeQty(item, 1)} aria-label={`Increase ${product.name}`} className="px-3 py-2">+</button></div><button onClick={() => setCart((items) => items.filter((entry) => entry !== item))} className="text-xs underline text-black/45">Remove</button></div></div></div>; }) : <p className="text-black/50">Your bag is empty.</p>}</div><div className="border-t border-black/15 pt-5"><div className="flex justify-between"><span>Subtotal</span><strong>{rupiah(subtotal)}</strong></div><p className="text-xs text-black/45 mt-2">Shipping calculated at checkout.</p><button disabled={!cart.length} onClick={() => showView("checkout")} className="w-full bg-ink text-white py-5 mt-5 disabled:opacity-40">Checkout — {rupiah(subtotal)}</button></div></aside></>}
    </div>}
  </>;
}
